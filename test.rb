#File.open("log/test.log", 'a') { |file| file.write("test") }
#class ApplicationController < ActionController::Base

  #last_id = Attend.last.id

  #Attend.all.each do |a|
    #id = a.id

    #unless id == last_id
      #unless Attend.find(id+1).nil?
        #b = Attend.find(id+1)
        #if a.rocket_member_id == b.rocket_member_id && a.date == b.date
          #b.destroy
        #end
      #end
    #end
  #end
#end
