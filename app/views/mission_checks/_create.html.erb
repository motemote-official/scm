<%= link_to "성공", rocket_mission_checks_path(rocket.id, mission_id: mission.id, rocket_member_id: m.id, status: 1), method: "POST", remote: true, id: "#{m.id}-mission-#{mission.id}", class: "mission-btn #{MissionCheck.where(mission_id: mission.id, rocket_member_id: m.id).present? ? "status" : ""}" %>
<%= link_to "실패", rocket_mission_checks_path(rocket.id, mission_id: mission.id, rocket_member_id: m.id, status: 0), method: "POST", remote: true, id: "#{m.id}-mission-#{mission.id}", class: "mission-btn #{MissionCheck.where(mission_id: mission.id, rocket_member_id: m.id).present? ? "" : "status"}" %>
